void main() {
  const range = 5;
  List<dynamic> numbers = Iterable.generate(range).toList();
  var empty = Iterable.empty();
  List<dynamic> _empty = Iterable.empty().toList();

  List<String> dictionary = ['Foo', 'Bar', 'lol'];
}
